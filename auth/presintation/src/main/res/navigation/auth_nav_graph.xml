<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/auth_nav_graph"
    app:startDestination="@id/authFragment">

    <fragment
        android:id="@+id/authFragment"
        android:name="com.example.presintation.fragment.authfragment.AuthFragment"
        android:label="fragment_auth"
        tools:layout="@layout/fragment_auth">
        <action
            android:id="@+id/action_authFragment_to_registerBottomSheetFragment"
            app:destination="@id/registerBottomSheetFragment" />
    </fragment>
    <deepLink app:uri="notelist://auth" />
    <dialog
        android:id="@+id/registerBottomSheetFragment"
        android:name="com.example.presintation.fragment.registerfragment.RegisterBottomSheetFragment"
        android:label="fragment_register_bottom_sheet"
        tools:layout="@layout/fragment_register_bottom_sheet" />
</navigation>